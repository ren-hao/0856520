language: java
jdk:
  - oraclejdk8
dist: trusty

before_install:
  - chmod +x gradlew

script:
  - ./gradlew build

jobs:
  include:
    - stage: Custom Testing
      name: Unit-Testing
      script: ./gradlew clean test --info